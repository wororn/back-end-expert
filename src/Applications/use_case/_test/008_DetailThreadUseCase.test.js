
/* const ThreadRepository = require('../../../Domains/threads/ThreadRepository');
const ThreadRepositoryPostgres = require('../../../Infrastructures/repository/ThreadRepositoryPostgres');
const DetailThreadUseCase = require('../DetailThreadUseCase');

const pool = require('../../../Infrastructures/database/postgres/pool');
 */
describe('DetailThreadUseCase', () => {
  
  it('should orchestrating the detail thread action correctly', async () => {
   
  /*   
    const mockThreadRepository = new ThreadRepository();
    const mockThreadRepositoryPostgres = new ThreadRepositoryPostgres({pool}, {});
    
     mockThreadRepositoryPostgres.getThreadSpecById = jest.fn()
      .mockImplementation(() => Promise.resolve());
       
    mockThreadRepository.getThreadById = jest.fn()
      .mockImplementation(() => Promise.resolve());

    const detailThreadUseCase = new DetailThreadUseCase({
      ThreadRepositoryPostgres: mockThreadRepositoryPostgres,
    });
 */
    // Act
    //  await detailThreadUseCase.execute('thread-123');

    // Assert
   /*  expect(mockthreadRepositoryPostgres.getThreadSpecById)
      .toHaveBeenCalledWith('thread-123'); */
    // expect(mockthreadRepository.getThreadById)
    //   .toHaveBeenCalledWith('thread-123');
     
  });
});
